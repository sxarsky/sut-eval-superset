# Generated by Skyramp v1.3.30 on 2026-06-30 00:06:49.238104059 +0000 UTC m=+321.505012069
# Command: skyramp generate contract rest http://localhost:8088/api/v1/me/ \
# 		--auth-type Bearer \
# 		--framework pytest \
# 		--language python \
# 		--method GET \
# 		--output get_me_contract_test.py \
# 		--output-dir /home/runner/work/sut-eval-superset/sut-eval-superset/tests/skyramp \
# 		--overwrite \
# 		--provider \
# 		--response-data {"result":{"id":1,"username":"admin","email":"admin@example.com","first_name":"Admin","last_name":"User","is_active":true,"is_anonymous":false,"login_count":5,"edition":"community"}} \
# 		--response-status-code 200 \
# 		--runtime local

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:8088")

# contract test for /api/v1/me/ GET
def test_me_get():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Expected Response Body
    expected_me_GET_response_body = r'''{
        "result": {
            "edition": "community",
            "email": "admin@example.com",
            "first_name": "Admin",
            "id": 1,
            "is_active": true,
            "is_anonymous": false,
            "last_name": "User",
            "login_count": 5,
            "username": "admin"
        }
    }'''

    # Execute Request
    me_GET_response = client.send_request(
        url=URL,
        path="/api/v1/me/",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert me_GET_response.status_code == 200
    assert skyramp.check_schema(me_GET_response, expected_me_GET_response_body)
    assert skyramp.get_response_value(me_GET_response, "result.edition") is not None
    assert skyramp.get_response_value(me_GET_response, "result.email") is not None
    assert skyramp.get_response_value(me_GET_response, "result.first_name") is not None
    assert skyramp.get_response_value(me_GET_response, "result.edition") == "community"
    assert skyramp.get_response_value(me_GET_response, "result.is_active") == True
    assert skyramp.get_response_value(me_GET_response, "result.is_anonymous") == False
    assert skyramp.get_response_value(me_GET_response, "result.id") > 0
    assert skyramp.get_response_value(me_GET_response, "result.login_count") >= 0


if __name__ == "__main__":
    test_me_get()
