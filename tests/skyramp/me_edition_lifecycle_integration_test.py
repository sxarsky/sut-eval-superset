# Generated by Skyramp v1.3.30 on 2026-06-30 00:06:21.719492881 +0000 UTC m=+293.986400891
# Command: skyramp generate integration rest \
# 		--auth-type Bearer \
# 		--framework pytest \
# 		--language python \
# 		--output me_edition_lifecycle_integration_test.py \
# 		--output-dir /home/runner/work/sut-eval-superset/sut-eval-superset/tests/skyramp \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/sut-eval-superset/sut-eval-superset/tests/skyramp/scenario_me_edition_lifecycle.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:8088")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Execute Request
    endpoint_1_GET_response = client.send_request(
        url=URL_localhost,
        path="/api/v1/me/",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert endpoint_1_GET_response.status_code == 200
    assert skyramp.get_response_value(endpoint_1_GET_response, "result.edition") == "community"
    assert skyramp.get_response_value(endpoint_1_GET_response, "result.is_active") == True
    assert skyramp.get_response_value(endpoint_1_GET_response, "result.is_anonymous") == False
    assert skyramp.get_response_value(endpoint_1_GET_response, "result.id") > 0
    assert skyramp.get_response_value(endpoint_1_GET_response, "result.login_count") >= 0

    # Fetch CSRF token required for PUT requests
    csrf_response = client.send_request(
        url=URL_localhost,
        path="/api/v1/security/csrf_token/",
        method="GET",
        headers=headers
    )
    csrf_token = skyramp.get_response_value(csrf_response, "result")
    if csrf_token:
        headers["X-CSRFToken"] = csrf_token

    # Request Body
    endpoint_1_PUT_request_body = r'''{
        "first_name": "UpdatedFirst"
    }'''

    # Execute Request
    endpoint_1_PUT_response = client.send_request(
        url=URL_localhost,
        path="/api/v1/me/",
        method="PUT",
        body=endpoint_1_PUT_request_body,
        headers=headers
    )

    # Generated Assertions
    assert endpoint_1_PUT_response.status_code == 200
    assert skyramp.get_response_value(endpoint_1_PUT_response, "result.edition") == "community"
    assert skyramp.get_response_value(endpoint_1_PUT_response, "result.first_name") == "UpdatedFirst"
    assert skyramp.get_response_value(endpoint_1_PUT_response, "result.is_active") == True
    assert skyramp.get_response_value(endpoint_1_PUT_response, "result.is_anonymous") == False
    assert skyramp.get_response_value(endpoint_1_PUT_response, "result.id") == skyramp.get_response_value(endpoint_1_GET_response, "result.id")

    # Execute Request
    endpoint_1_GET_1_response = client.send_request(
        url=URL_localhost,
        path="/api/v1/me/",
        method="GET",
        headers=headers
    )

    # Generated Assertions
    assert endpoint_1_GET_1_response.status_code == 200
    assert skyramp.get_response_value(endpoint_1_GET_1_response, "result.edition") == "community"
    assert skyramp.get_response_value(endpoint_1_GET_1_response, "result.first_name") == "UpdatedFirst"
    assert skyramp.get_response_value(endpoint_1_GET_1_response, "result.is_active") == True
    assert skyramp.get_response_value(endpoint_1_GET_1_response, "result.id") == skyramp.get_response_value(endpoint_1_GET_response, "result.id")


if __name__ == "__main__":
    test_integration()
