# Generated by Skyramp v1.3.30 on 2026-06-30 00:06:19.370237025 +0000 UTC m=+291.637145035
# Command: skyramp generate contract rest http://localhost:8088/api/v1/me/ \
# 		--auth-type Bearer \
# 		--framework pytest \
# 		--language python \
# 		--method PUT \
# 		--output-dir /home/runner/work/sut-eval-superset/sut-eval-superset/tests/skyramp \
# 		--overwrite \
# 		--provider \
# 		--request-data {"first_name":"TestFirst"} \
# 		--response-data {"result":{"id":1,"username":"admin","email":"admin@example.com","first_name":"TestFirst","last_name":"User","is_active":true,"is_anonymous":false,"login_count":5,"edition":"community"}} \
# 		--response-status-code 200 \
# 		--runtime local

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:8088")

# contract test for /api/v1/me/ PUT
def test_me_put():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Fetch CSRF token required for PUT requests
    csrf_response = client.send_request(
        url=URL,
        path="/api/v1/security/csrf_token/",
        method="GET",
        headers=headers
    )
    csrf_token = skyramp.get_response_value(csrf_response, "result")
    if csrf_token:
        headers["X-CSRFToken"] = csrf_token

    # Request Body
    me_PUT_request_body = r'''{
        "first_name": "TestFirst"
    }'''

    # Expected Response Body
    expected_me_PUT_response_body = r'''{
        "result": {
            "edition": "community",
            "email": "admin@example.com",
            "first_name": "TestFirst",
            "id": 1,
            "is_active": true,
            "is_anonymous": false,
            "last_name": "User",
            "login_count": 5,
            "username": "admin"
        }
    }'''

    # Execute Request
    me_PUT_response = client.send_request(
        url=URL,
        path="/api/v1/me/",
        method="PUT",
        body=me_PUT_request_body,
        headers=headers
    )

    # Generated Assertions
    assert me_PUT_response.status_code == 200
    assert skyramp.check_schema(me_PUT_response, expected_me_PUT_response_body)
    assert skyramp.get_response_value(me_PUT_response, "result.edition") is not None
    assert skyramp.get_response_value(me_PUT_response, "result.email") is not None
    assert skyramp.get_response_value(me_PUT_response, "result.first_name") is not None
    assert skyramp.get_response_value(me_PUT_response, "result.edition") == "community"
    assert skyramp.get_response_value(me_PUT_response, "result.first_name") == "TestFirst"
    assert skyramp.get_response_value(me_PUT_response, "result.is_active") == True
    assert skyramp.get_response_value(me_PUT_response, "result.is_anonymous") == False
    assert skyramp.get_response_value(me_PUT_response, "result.username") == "admin"
    assert skyramp.get_response_value(me_PUT_response, "result.id") > 0
    assert skyramp.get_response_value(me_PUT_response, "result.login_count") >= 0


if __name__ == "__main__":
    test_me_put()
